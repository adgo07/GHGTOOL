# 青舟温室气体排放核算软件 Windows V1 分阶段开发 HANDOFF

**文档版本：** V1.0  
**冻结日期：** 2026-09-10  
**决策与验收：** GPT-5.6 Sol  
**执行模型：** GPT-5.6 Luna Max（Goal 模式）  
**项目目录：** `D:\project\碳排放核算工具`  
**当前首个核算标准：** GB/T 32151.34—2024  

## 1. 文档用途

本文件是 Luna Max 分阶段实施 Windows V1 的唯一当前交接基线。执行模型不得依赖此前聊天内容，不得重新设计已经冻结的产品方向。

本项目采用“一个阶段一个 Goal”的方式。每个阶段完成后必须停止，提交 `IMPLEMENTATION_REPORT.md`，等待 Sol 验收。只有验收结论为 `PASS`，或 `PASS WITH MINOR FIXES` 且修正完成后，才能由用户启动下一阶段 Goal。

## 2. 权威文件和冲突顺序

### 2.1 必读文件

1. `D:\project\碳排放核算工具\AGENTS.md`
2. `D:\project\碳排放核算工具\HANDOFF.md`
3. `D:\project\碳排放核算工具\TASK_STATE.md`
4. `D:\MD仓库\杂\软件公共框架规范\三套工业能源软件公共框架规范 V1.0——首页和导航部分.md`
5. `D:\MD仓库\杂\软件公共框架规范\三类专业软件统一工程架构与跨平台开发 Handoff.md`
6. `D:\MD仓库\杂\碳排放计算软件\首页和导航设计 Handoff.md`
7. `D:\MD仓库\杂\碳排放计算软件\标准库设计.md`
8. `D:\MD仓库\杂\碳排放计算软件\新建核算设计.md`
9. `D:\MD仓库\杂\碳排放计算软件\参数与排放因子库完整开发 Handoff.md`
10. `D:\MD仓库\杂\碳排放计算软件\GB T 32150—2025 通用规则映射方案.md`
11. `D:\MD仓库\杂\碳排放计算软件\GB T 32151.34—2024 炭素材料生产企业映射方案.md`
12. `D:\MD仓库\杂\碳排放计算软件\GWP数据库.md`

### 2.2 冲突处理

- 安全、执行纪律和模型职责以 `AGENTS.md` 为准。
- 本轮产品范围、已批准例外和阶段划分以本 `HANDOFF.md` 为准。
- 公共视觉与导航以公共框架规范为准。
- 碳核算专项页面、字段和业务交互以对应专项设计为准。
- 核算公式和标准解释以两份正式映射方案及可核对的标准原文为准。
- `第一次沟通.md` 和 `资料缺口清单.md` 是历史资料，不是实施基线；其中“映射表为 0”等状态已经过时。
- 遇到不能按以上顺序消解的冲突，按 `BLOCKED` 格式上报。

## 3. 项目目标

交付一个可安装、可离线运行的 Windows x64 桌面软件基础版本，完成以下闭环：

```text
启动软件
→ 首页与导航
→ 标准库/参数与排放因子库
→ 选择 GB/T 32151.34—2024
→ 手工输入最小核算信息和活动数据
→ 加载可追溯参数
→ 校验
→ 高精度计算
→ 自动生成不可编辑核算记录
→ 在核算记录中查看输入、计算结果、规则版本和参数快照
```

本轮成功的定义是“首个标准的可信计算与记录闭环”，不是完成全部八项标准。

## 4. 已冻结的产品决定

### 4.1 产品范围

- 当前只生成一个碳核算 Windows 程序，不建立另外两个空壳 EXE。
- 工程结构保留未来 Monorepo 和公共 packages 的扩展位置。
- 首个且唯一可计算标准为 GB/T 32151.34—2024。
- GB/T 32150—2025作为通用规则层，不作为独立行业核算页面。
- GB/T 32151.1、.4、.5、.7、.8、.13、.41只允许出现在标准目录中，并明确显示“核算模块待开发”。

### 4.2 数据存储

采用三个物理隔离的 SQLite 数据库：

```text
catalog.sqlite
  官方标准、来源、参数、排放因子、GWP、标准版本和数据版本

user.sqlite
  用户设置、窗口状态、最近操作和本地偏好

records.sqlite
  成功核算记录、输入快照、计算过程、参数快照和审计日志
```

`.qzproj` 在公共架构中继续保留为未来跨产品能力，但碳核算 V1 不实现项目文件入口，不实现保存项目，不实现草稿。

### 4.3 记录生命周期

- 未点击计算前，输入只存在页面内存中。
- 离开含有未计算输入的页面时提示数据将丢失。
- 致命校验失败：不执行完整计算，不生成记录。
- 成功计算：立即写入一条新记录。
- 有非致命警告时：写入 `COMPLETED_WITH_WARNINGS`。
- 无警告时：写入 `COMPLETED`。
- 历史记录只读，不允许原地修改或覆盖。
- 删除必须二次确认，采用可追溯的逻辑删除或等效审计机制，保留删除时间和动作记录。

### 4.4 精度和单位

- 输入数值先以字符串接收并严格解析，禁止先转二进制浮点再进入领域层。
- Domain 统一使用十进制高精度类型；默认上下文精度至少 28 位有效数字。
- 中间步骤不舍入，除非适用标准明确要求。
- 持久化保存未舍入结果；界面默认显示 2 位小数。
- 默认舍入方式为四舍五入到最接近值，正中间向远离零方向处理；标准有明确规定时由标准模块覆盖并记录规则版本。
- 单位换算只能通过 `UnitService`；算法模块不得散落手写换算常数。
- 首批至少覆盖 kg/t、kWh/MWh、kJ/GJ、Nm³/10⁴Nm³、百分数/比例以及 C→CO₂ 的 44/12。

### 4.5 标准和参数来源

- JSON/YAML 是唯一 Canonical Source，SQLite 必须由构建脚本生成。
- 每条官方数据至少具有稳定 ID、值、单位、来源文件、条款/表格定位、版本、有效期、适用范围、审核状态。
- 标准记录必须包含 `official_source_url`；无法确认官方网址时按钮禁用，不得使用搜索结果页或非官方网站代替。
- 标准全文和本地 PDF 不进入程序包。
- 历史记录保存本次实际采用值，而不是只保存因子 ID。
- GB/T 32151.34 当前只涉及 CO₂；不得为了展示 GWP 功能强行改变其计算口径。

### 4.5.1 多种电力消费形式与非化石能源电力参数（2026-09-13批准）

同一企业、同一核算期可以同时存在多种电力消费形式，界面和领域模型不得将企业电力消费设计为单选类型。电力消费必须采用“多条明细”结构，每条明细独立记录：电量；取得方式（外购、自发自用）；电力属性（常规电力、非化石能源电力）；核算期间；排放因子及来源；证明材料类型和证明状态；参数选择理由；独立参数快照。

“取得方式”和“电力属性”是两个不同维度，不得把“自发自用”直接等同于“非化石能源”。用户所说的“外购化石能源电力”，软件界面统一表述为“外购常规电力（电网电力）”，避免把电网混合电力错误表述为纯化石能源电力。

参数与校验规则冻结如下：

- 外购常规电力（电网电力）：采用适用的最新全国电力平均二氧化碳排放因子。
- 外购非化石能源电力：GB/T 32151.34—2024 附录 D.1.1 规定因子为零；使用零因子的前置条件是具有附录 D.2 要求的交易合同及结算凭证或 GEC 等证明。
- 自发自用非化石能源电力：因子为零；使用零因子的前置条件是具有附录 D.2 要求的月度电量原始记录。
- 自发自用化石能源电力：不得作为外购电力间接排放重复计算；相关燃料排放进入直接排放路径，并设置防重复计算校验。
- 缺少适用证明时不得采用零因子，也不得静默回退到全国平均因子，必须产生 `ERROR`。

批准在 Canonical Source 中新增彼此独立的非化石能源电力参数与因子，不得挂到现有 `electricity_emission_factor_national` 参数下：

|字段|批准值|
|---|---|
|`parameter_id`|`electricity_emission_factor_nonfossil`|
|`factor_id`|`electricity_nonfossil_zero_gbt32151_34_2024`|
|值、源值、规范化值|字符串 `"0"`|
|单位|`tCO₂/MWh`|
|`value_type`|`STANDARD_SPECIFIED`|
|`review_status`|`VERIFIED`|
|`source_id`|`SRC-32151-34-2024`|
|`source_location`|`GB/T 32151.34—2024 第5.2.6.1条、附录D.1.1；PDF第30页；印刷页22`|
|`factor_year`|`2024`|
|`valid_from`|`2025-03-01`|
|适用标准|仅 `gbt_32151_34_2024`|

该 Canonical 记录的备注必须明确：附录 D.2 证明是使用零因子的前置条件。全国平均因子与非化石能源零因子是两个不同业务参数，不得表达为同一参数的不同版本。

### 4.6 暂不支持的活动

当炭素企业存在本标准未覆盖的其他生产活动、上下游运输或需要调用其他行业标准时：

1. 明确提示所需标准或“需要其他标准核算”；
2. 不自动套用近似公式；
3. 不并入 GB/T 32151.34 当前结果；
4. 不因缺少其他模块扩大本阶段 Scope。

### 4.7 品牌和界面

- 使用 `D:\新建文件夹\logo\青舟节能  --转曲.png` 作为正式品牌源图。
- 原图约 3060×759，按约 4.03:1 比例缩放，不拉伸、不裁切。
- 左侧品牌区宽度和显示尺寸以公共框架最新规范为准。
- Excel 导入入口保留，控件全部禁用，并显示“功能将在后续版本开放”。
- 报告/导出如必须出现在既有设计页面中，只保留禁用占位，不实现导出逻辑。

## 5. 明确不做的内容

以下全部为 OUT OF SCOPE：

- 其余七项行业标准的输入页面、公式、参数选择和计算；
- 报告模板、Word/PDF/Excel导出和打印；
- Excel 数据导入解析、映射和执行；
- 完整企业档案、企业层级、审批和多用户协作；
- 企业真实输入基准、黄金算例和客户验收数据；
- `.qzproj` 项目保存与打开；
- 云端、账号、在线同步、在线许可证服务器；
- 自动联网抓取标准、OCR、AI 自动判断冲突；
- 完整标准全文内置；
- 另外两套工业能源软件的应用实现；
- 为未来功能预先制作复杂通用框架；
- 未经批准升级主要技术栈或改变核心目录职责。

说明：暂缓黄金算例不等于允许不测试。每阶段仍必须有工程单元测试、组件测试和最小回归测试，只是不使用企业真实输入作为业务正确性基准。

## 6. 总体工程边界

建议目录骨架：

```text
D:\project\碳排放核算工具\
├─ AGENTS.md
├─ HANDOFF.md
├─ TASK_STATE.md
├─ IMPLEMENTATION_REPORT.md
├─ pyproject.toml
├─ apps/
│  └─ carbon_accounting_desktop/
├─ packages/
│  ├─ core/
│  ├─ ui/
│  ├─ standards/
│  ├─ reference_data/
│  ├─ persistence/
│  └─ audit/
├─ specs/
│  ├─ common/
│  └─ carbon_accounting/
├─ data-source/
│  ├─ common/
│  └─ carbon_accounting/
├─ resources/
├─ migrations/
├─ scripts/
├─ tests/
├─ docs/
├─ build/
└─ 计算表/                 # 用户参考资料，只读，不修改
```

允许 Luna 在不改变职责的情况下微调 Python 包名，但不得把所有代码塞进单个应用文件。

## 7. 分阶段 Goal 总表

|阶段|Goal编号|成果|进入条件|
|---|---|---|---|
|阶段0|G00|治理文件、工程基线和可启动空应用|本 HANDOFF 已批准|
|阶段1|G01|Domain基础、DecimalPolicy、UnitService、Repository契约|G00 PASS|
|阶段2|G02|Canonical数据、三库结构、迁移与目录构建|G01 PASS|
|阶段3|G03|公共桌面外壳、首页、导航、Logo和禁用入口|G02 PASS|
|阶段4|G04|标准库、参数与排放因子库查询页面|G03 PASS|
|阶段5|G05|通用规则解析、参数推荐和计算快照基础|G04 PASS|
|阶段6|G06|GB/T 32151.34输入、校验与计算模块|G05 PASS|
|阶段7|G07|自动记录、只读查看、警告状态和审计删除|G06 PASS|
|阶段8|G08|全链路集成、回归、Windows构建和交付整理|G07 PASS|

严禁把多个阶段合并成一个长 Goal。每个阶段结束后必须停止。

## 8. 阶段0：G00 工程治理与最小可启动骨架

### Goal 目标

建立可持续分阶段开发的仓库基线和最小可启动空应用，不实现业务页面。

### MUST

- 检查工作区现状，保护 `计算表/` 和用户已有文件。
- 如尚无 Git 仓库，建立本地 Git 仓库；不得删除或自动提交用户参考表。
- 建立 `pyproject.toml`、目录骨架、应用入口、基础配置和测试目录。
- 采用 Python 3.12 x64；如当前环境或 PySide6 明确不兼容，停止并上报，不得静默切换主要版本。
- 建立最小 PySide6 窗口，使应用能启动和正常退出。
- 将品牌 Logo 复制到项目资源目录，保留源比例；不得修改外部源文件。
- 建立结构化日志基础，但不得记录活动数据、企业信息或许可证等敏感明文。
- 初始化 `TASK_STATE.md` 的真实状态。

### OUT OF SCOPE

- 数据库业务表；
- 正式首页；
- 标准和计算功能；
- 打包安装程序。

### 验收

- 项目能在干净环境按说明安装依赖。
- 应用最小窗口能启动并退出。
- 基础测试可运行且通过。
- Domain/package骨架没有反向依赖UI。
- `计算表/` 未被修改。
- `IMPLEMENTATION_REPORT.md` 记录实际命令和 Git 状态。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G00 阶段。只建立工程治理、目录结构、依赖配置、最小可启动 PySide6 空应用、Logo资源副本和基础测试。不得开始 G01 或任何业务页面。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 9. 阶段1：G01 领域基础与公共契约

### Goal 目标

建立不依赖桌面UI和SQLite的业务基础，使后续标准模块不会把公式写进页面。

### MUST

- 实现 `DecimalPolicy`，满足第4.4节已冻结规则。
- 实现 `UnitService` 和首批单位定义、量纲检查、可逆转换。
- 建立标准、来源、参数、因子、核算输入、校验问题、计算结果、参数快照、核算记录的领域模型。
- 建立 Standard、Parameter、Record、Settings Repository 抽象接口。
- 统一错误级别：`ERROR`、`WARNING`、`INFO`；只有 ERROR 阻止记录生成。
- 建立稳定ID和版本字段约定；领域对象禁止使用页面显示文字作为主键。
- 为精度、单位和领域规则写单元测试。

### 禁止

- Domain 导入 PySide6、sqlite3或Windows API。
- 在页面或数据库代码中实现计算公式。
- 引入尚未批准的通用规则引擎产品。

### 验收

- 精度、舍入和单位边界测试通过。
- Repository可以使用内存假实现进行测试。
- 领域层可以在没有GUI和数据库的环境单独运行。
- 不存在散落的单位魔法数字。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G01 阶段。只实现 DecimalPolicy、UnitService、核心领域模型、错误级别和 Repository 契约及其测试。不得创建正式数据库页面，不得开始 G02。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 10. 阶段2：G02 Canonical数据与SQLite基础

### Goal 目标

把已批准的标准、参数和来源设计转化为可校验的机器数据，并建立三个隔离数据库。

### MUST

- 为 Canonical JSON/YAML 建立 schema 和校验脚本。
- 建立 `catalog.sqlite`、`user.sqlite`、`records.sqlite` 独立迁移。
- 建立从 Canonical Source 校验、标准化并生成 `catalog.sqlite` 的可重复构建流程。
- 首批录入 GB/T 32150—2025、GB/T 32151.34—2024及其明确使用的来源、参数、单位换算和CO₂ GWP=1。
- 其余七项标准只录入经官方来源核对的目录元数据、状态与官方URL；不得录入计算规则。
- 无法核对的字段使用明确的空值/待核对状态，前端相关功能禁用，不得编造。
- 实现应用版本、schema版本、data版本相互独立。
- 提供数据库全新创建和迁移测试。

### 数据纪律

- 标准原文不复制进 Canonical Source。
- 每个数值必须具有来源定位和审核状态。
- 不把示例值、设计稿文字或搜索摘要当作正式值。
- GWP首批只录入首个模块及公共结构所需最小集合；完整AR6表后续补充。

### 验收

- 删除生成数据库后可从Canonical数据完全重建。
- Schema错误、单位错误、重复稳定ID和缺失来源能阻止构建。
- 三个数据库不存在职责混放。
- 标准原文不在构建产物中。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G02 阶段。建立 Canonical JSON/YAML schema、校验和 catalog构建流程，建立 catalog/user/records 三套独立SQLite迁移，并录入经来源核对的最小数据。其余七个标准只做目录元数据，不能做计算规则。不得开始 G03。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 11. 阶段3：G03 公共桌面外壳与导航

### Goal 目标

严格按公共框架和碳软件首页设计实现桌面外壳、首页和导航，不接入核算算法。

### MUST

- 实现统一窗口、左侧导航、顶部品牌区、内容区域和设置入口。
- 按规范使用Logo尺寸和比例。
- 实现首页空状态、最近记录/最近标准区域的安全空数据展示。
- 导航包含首页、新建核算、Excel导入、核算记录、标准库、参数与排放因子库、设置。
- Excel导入入口和页面保留，但全部控件禁用。
- 报告/导出入口若出现，只能禁用占位。
- 建立页面路由和视图模型，不把数据库查询直接写进控件事件。
- 支持 Windows 11 x64，兼容目标 Windows 10 22H2 x64。

### 验收

- 所有导航可到达预期页面；禁用项无法触发业务动作。
- 窗口缩放后Logo不变形，导航和内容不重叠。
- 空数据库启动无异常。
- GUI最小冒烟测试通过。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G03 阶段。只实现公共桌面外壳、首页、导航、Logo、空状态和Excel导入禁用占位；不实现核算算法、报告或导出。不得开始 G04。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 12. 阶段4：G04 标准库与参数因子库页面

### Goal 目标

实现只读、可追溯的标准和参数查询体验，为计算模块提供统一数据入口。

### MUST

- 实现标准列表、搜索、筛选、详情和状态显示。
- GB/T 32151.34显示可进入核算；其他七项显示“核算模块待开发”且入口禁用。
- “打开标准原文”调用系统默认浏览器打开 `official_source_url`；URL缺失时按钮禁用。
- 实现参数与排放因子库的全局搜索、按对象、按来源、详情和来源追溯。
- 同一参数多个版本并存，明确显示推荐值、其他适用值、历史值和审核状态。
- 页面只通过Application Service/Repository读取数据。

### 禁止

- 直接打开本地标准PDF。
- 在页面中写死标准状态、参数值或推荐逻辑。
- 为补齐展示而制造假数据。

### 验收

- 搜索编号、燃料、参数和来源能返回正确结果。
- 状态由官方状态和日期规则产生，不是写死文字。
- URL按钮和核算入口的可用状态正确。
- 数据库缺少非必需字段时页面安全降级。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G04 阶段。实现只读标准库和参数与排放因子库页面、搜索筛选、详情、状态、官方URL跳转及禁用核算入口。不得实现其他七个标准，不得开始 G05。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 13. 阶段5：G05 通用规则、推荐值与快照基础

### Goal 目标

实现 GB/T 32150 通用规则层、有效规则解析、参数推荐和可追溯快照，不制作炭素输入页面。

### MUST

- 按映射方案实现 `EffectiveRuleResolver` 及 BASE、SPECIALIZE、OVERRIDE、EXTEND、SUPPLEMENT、CONFLICT_REVIEW。
- 未解决的 `CONFLICT_REVIEW` 必须阻止成功核算。
- 实现标准、核算期间、地区、对象、来源类型等上下文驱动的参数解析服务。
- 行业标准明确值优先于通用更新值；不能唯一确定时要求用户确认并保存理由。
- 建立本次实际采用值、单位、来源、版本、选择方法和理由的不可变快照对象。
- 实现通用活动数据、来源、校验和聚合契约。
- 通过内存Repository完成规则与推荐服务测试。
- 按第 4.5.1 节补录独立的 Canonical 非化石能源电力参数与零因子，并保持 JSON/YAML 为唯一事实源、SQLite 可重复重建。
- 建立多条电力消费明细所需的领域上下文和参数解析能力；每条明细必须独立解析、独立校验并形成独立参数快照，不能相互覆盖。
- G05 不制作电力录入页面，不计算企业电力排放总量。

### 验收

- 优先级、覆盖、补充和冲突路径测试通过。
- 参数库更新不会改变已创建的快照对象。
- Domain服务不依赖UI或SQLite。
- 未确认参数不会被静默选择。
- 组合场景测试通过：同一企业、同一核算期同时存在外购常规电力、外购非化石能源电力和自发自用非化石能源电力；三条数据同时存在，分别解析、分别校验、分别形成快照且互不覆盖。
- 缺少附录 D.2 适用证明时产生 `ERROR`，既不采用零因子，也不静默回退全国平均因子。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G05 阶段。实现 GB/T 32150 通用规则解析、动态参数推荐、冲突处理和不可变参数快照基础；按第4.5.1节补录独立的非化石能源电力 Canonical 参数与零因子，并实现多条电力明细的独立解析、校验和快照组合测试。不制作电力录入页面，不计算企业电力排放总量，不得开始 G06。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 14. 阶段6：G06 GB/T 32151.34 核算模块

### Goal 目标

实现首个标准的手工录入、适用性判断、专业校验和计算，但暂不把结果持久化为正式记录。

### MUST

- 按正式映射方案实现 CAR-F01、CAR-P01、CAR-P02、CAR-P03、CAR-P04A、CAR-P04B、CAR-I01、CAR-I02、CAR-I03、CAR-I04。
- 实现映射方案中的公式、单位、默认参数、输入路径互斥、物料平衡和校验编号。
- 实现直接排放、间接排放和总排放的标准专属聚合规则。
- 月度核算标识为内部周期结果；年度核算才是标准周期。
- 页面采用统一外层框架加标准专属长页面，排放源可以标记“不涉及”。
- 最小身份信息只要求企业名称和核算期间；不建立企业档案和企业层级系统。
- 参数选择器接入 G05 服务，显示来源和选择状态。
- 成功计算返回完整领域结果、校验问题、算法版本和参数快照，但本阶段使用内存Record Repository验证，不落正式业务记录。
- 其他行业活动和运输按第4.6节阻止并提示。
- 电力消费采用可增加、删除的多条明细；每条明细独立选择“取得方式”和“电力属性”，不得使用单选结构限制企业只能存在一种电力形式。
- 每条电力明细分别计算后再汇总；不得让一条明细的参数、证明状态或快照覆盖另一条明细。
- 建立防重复计算校验，防止同一电量在常规电力、非化石能源电力或自发自用燃料直接排放路径中被重复计入。

### 公式纪律

- 映射文件中的公式、变量含义、默认值和校验属于冻结业务口径。
- 发现映射与标准原文疑似不一致时必须 BLOCKED，不得按经验改公式。
- 不得把通则总量公式强行覆盖炭素标准的式（14）～（16）。
- 不得重复计入燃料、BWT/GWT、石墨化炉体炭损或回收利用项。

### 验收

- 每个排放源至少具有正常路径、零值、缺失值、非法百分数、单位不匹配和“不涉及”工程测试。
- 默认值启用时有明确INFO/WARNING并进入结果快照。
- 任一ERROR均阻止成功结果。
- UI不包含公式实现；相同Domain输入在无UI环境返回相同结果。
- 暂不使用企业真实数据或黄金算例作为验收条件。
- 同一企业、同一核算期可同时录入外购常规电力、外购非化石能源电力和自发自用非化石能源电力，并按明细分别计算后正确汇总。
- 自发自用化石能源电力进入燃料直接排放路径且不重复计入外购电力间接排放；重复数据必须被校验阻止。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G06 阶段。严格依据正式映射和第4.5.1节实现GB/T 32151.34手工录入页面、全部排放源、校验、参数选择和计算Domain服务；电力采用可增删的多条明细，取得方式与电力属性分开选择，逐条计算后汇总并防止重复计算。本阶段只用内存记录仓库验证，不实现正式记录持久化。遇到标准解释冲突立即BLOCKED。不得开始 G07。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 15. 阶段7：G07 核算记录与审计闭环

### Goal 目标

实现“计算即生成记录”的最终业务闭环。

### MUST

- 计算按钮先做完整校验，再以单一事务写入成功记录及全部快照。
- 保存核算身份、标准版本、算法版本、有效规则集、输入、排放源判断、结果、参数来源快照和警告。
- 实现 `COMPLETED`、`COMPLETED_WITH_WARNINGS` 两种成功状态。
- ERROR时不写成功记录；不得产生半条记录。
- 实现核算记录列表、搜索/基本筛选、只读详情。
- 重新计算创建新记录，旧记录不覆盖。
- 实现带二次确认和审计信息的删除；列表默认不显示已删除记录。
- 未计算输入离开页面时提示并丢弃。
- 首页最近记录从records库读取。

### OUT OF SCOPE

- 基于记录重新核算；
- 恢复已删除记录的UI；
- 报告与导出；
- 修改历史记录。

### 验收

- 记录事务完整性、失败回滚、不可变性和快照稳定性测试通过。
- 修改catalog当前参数后，历史记录展示不改变。
- 同一输入连续成功计算生成不同记录ID。
- 删除动作可审计，且不会物理破坏审计证据。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G07 阶段。实现计算成功即事务生成不可编辑记录、两种成功状态、参数与规则快照、记录列表和只读详情、审计删除及首页最近记录。不得实现导出、历史编辑或基于记录重算，不得开始 G08。完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，停止等待 Sol 验收。
```

## 16. 阶段8：G08 集成、回归与Windows交付

### Goal 目标

在不扩大业务范围的前提下完成全链路稳定性、Windows构建和使用说明。

### MUST

- 执行所有单元、组件、数据库迁移、GUI冒烟和全链路工程测试。
- 验证空库启动、首次建库、重复启动、数据库版本错误、安全失败和日志行为。
- 验证Windows 11 x64；有条件时验证Windows 10 22H2 x64，否则记录未验证原因。
- 生成standalone构建，不优先追求单文件EXE。
- 检查安装产物不包含标准全文、用户计算表、测试数据库或开发密钥。
- 提供安装、启动、数据目录、卸载后数据保留策略、当前限制和故障排查说明。
- 更新版本、schema版本、data版本和最终 `IMPLEMENTATION_REPORT.md`。

### OUT OF SCOPE

- 正式商业许可证和代码签名；
- 报告、导出、Excel导入；
- 企业真实数据验收；
- 其余七个核算标准；
- 在线更新服务和qzpack签名发布。

### 最终验收

- 新用户可以安装、启动并完成GB/T 32151.34手工核算和记录查看闭环。
- 禁用入口不会产生误操作。
- 历史记录可追溯到标准、算法、规则和参数快照。
- 所有已执行测试有真实记录；未执行项及原因明确。
- 构建产物经过内容检查。
- 工作区无未解释的临时代码、TODO、假数据和超Scope修改。

### Goal 启动文本

```text
执行 HANDOFF.md 的 G08 阶段。完成现有范围的全链路回归、Windows standalone构建、产物检查和使用文档。不得增加报告、导出、Excel导入、企业基准或其他标准。完成后更新 TASK_STATE.md 和最终 IMPLEMENTATION_REPORT.md，停止等待 Sol 最终验收。
```

## 17. 每个阶段统一执行流程

### 开始

1. 读取三层交接文件和当前阶段相关规范。
2. 检查Git、磁盘和测试真实状态。
3. 确认当前 `TASK_STATE` 与用户指定阶段一致。
4. 创建且只创建当前阶段Goal。
5. 简短报告预计修改范围和是否发现冲突。

### 实施

1. 把阶段拆成小工作包，但不得创建并行的其他阶段Goal。
2. 每个工作包完成后运行L1最小测试。
3. 相关工作包收口后运行L2回归。
4. 在稳定节点更新状态并形成Git checkpoint。
5. 发现阻塞立即记录，不用临时方案绕过。

### 结束

1. 运行本阶段L3验收。
2. 检查Scope和用户文件是否保持原样。
3. 更新 `TASK_STATE.md`。
4. 更新 `IMPLEMENTATION_REPORT.md`。
5. 仅在阶段Definition of Done全部满足时将该Goal标记完成。
6. 停止，不自动进入下一阶段。

## 18. 测试层级

- L1：当前函数、类、迁移或页面组件的最小测试。
- L2：当前阶段相关模块回归。
- L3：阶段验收；G08执行项目全量回归。

报告必须记录：测试命令、环境、通过数、失败数、跳过数、失败项、未执行项及原因。不得用手工点击代替所有自动测试；GUI视觉和操作检查可作为自动测试的补充。

## 19. Luna阶段交付格式

`IMPLEMENTATION_REPORT.md` 每阶段至少包含：

```md
# IMPLEMENTATION_REPORT

## 阶段
G00 / G01 / ...

## 本轮完成
- ...

## 未完成
- 无 / ...

## 与HANDOFF的偏差
- 无 / ...

## 修改文件
- ...

## 数据与算法说明
- ...

## 测试
### L1
命令：
结果：

### L2
命令：
结果：

### L3
命令：
结果：

## Git
- 分支：
- commit：
- git status：

## 已知问题
- 无 / ...

## 建议Sol重点复核
- ...
```

## 20. Definition of Done

单个阶段只有同时满足以下条件才算完成：

- 阶段所有MUST已实现；
- OUT OF SCOPE没有被实施；
- 相关测试真实执行并记录；
- 没有未说明的失败、TODO、假数据或临时绕过；
- 用户已有文件未被覆盖；
- `TASK_STATE.md`与实际状态一致；
- `IMPLEMENTATION_REPORT.md`完整；
- Git状态清楚，每项未提交变化都有解释；
- Luna已经停止，没有自行进入下一阶段。

整个Windows V1只有在G00～G08全部经Sol验收后才算完成。

## 21. Post-V1 新建核算 UI 重构 UIR01～UIR04

G00～G08 已完成 Windows V1 阶段门禁。以下是 V1 完成后的“新建核算”界面重构治理章节；它不改写 G00～G08 的历史交接记录，也不自动启动任何后续 Goal。

### 21.1 总边界

- 只重构 GB/T 32151.34—2024 当前“新建核算”页面的 Presentation 层和用户输入体验。
- 保持 CarbonMaterialInput、Domain Decimal 精度、GB/T 32151.34 公式、Canonical 数据、SQLite schema、records.sqlite 记录规则和 PR #2 的运行期间输入保留行为不变。
- 不修改、删除或上传 计算表/；不打包标准全文；不实施 Excel 导入、报告/导出、其他七项标准、云服务或 G09 类功能。
- UIR01～UIR04 必须按顺序执行；当前 Goal 未完成、未形成报告或存在 BLOCKED 时，不得进入下一阶段。

### 21.2 UIR01——字段语义层与类型化输入控件

#### MUST

1. 为当前页面所有用户输入建立可追溯的 Presentation FieldSpec，至少包含 internal_key、display_name、standard_symbol（如适用）、data_type、unit、min、max、required、help_text、standard_clause、source_location 和 advanced。
2. 普通用户界面不得显示 gc、wfc、cc、bpm、gpm、gpmvar 等内部变量名；中文名称必须来自已批准的标准映射或原始标准证据。无法可靠定位的字段必须按 BLOCKED 格式上报。
3. 提供可复用的数量、百分比/比例、整数、普通文本、枚举和只读标准参数控件/helper。数值控件即时拒绝字母、负值和超范围百分数；单位由字段定义控制，不得由用户自由填写。
4. 百分数 UI 使用 0～100，进入 Domain 前显式转换为 0～1 的 Decimal ratio，并测试 0%、100%、双向转换和既有计算结果等价。
5. UIR01 不进行 UIR02 的卡片化布局重构；只处理字段语义、label、输入类型和验证。

#### 测试与阶段门禁

- 必须有 FieldSpec 完整性、数值输入、ratio 转换和“同一合法输入映射为等价 Domain Input/计算结果”的定向测试。
- 必须回归 test_g06_page.py、test_g06_carbon_material.py、test_g07_records.py 和 test_g08_delivery.py，并记录全量测试、compileall、pip check 及必要的数据校验。
- 完成后更新 TASK_STATE.md 和 IMPLEMENTATION_REPORT.md，提交并停止等待 Sol 验收；不得创建或实施 UIR02。

### 21.3 UIR02——排放源与活动数据卡片重构（当前阶段）

#### Goal

在不改变 Domain、计算和记录语义的前提下，将“排放源识别”和“活动数据”合并为统一的“排放源与活动数据”页面，使用统一、可展开/折叠的 Presentation 排放源业务卡片完成十个排放源的状态识别、活动数据录入、轻量完成度反馈和输入保持。本阶段只处理页面结构、业务分组和排放源卡片交互，不启动 UIR03。

#### 模型边界

- 现有 `EmissionSourceStatus` 和 `CarbonMaterialInput.source_states` 保持不变，不为卡片 UI 新增 Domain 枚举或数据库字段。
- “不涉及 / 涉及 / 待确认”分别映射现有 `NOT_INVOLVED / INVOLVED / UNCONFIRMED`；“填写中 / 已完成 / 有错误或需要处理”是根据当前输入完整性和已有校验结果派生的 Presentation 状态，不得持久化为 Domain 状态。
- 卡片的展开、折叠、编辑只改变 Presentation；只有明确的启用/改为不涉及业务动作才能改变 Domain source state。

#### MUST

1. 将当前页面一级结构中的“03 排放源识别”和“04 活动数据”合并为“02 排放源与活动数据”。基本信息与核算边界可以重新排版，但不得改变业务含义、必填规则或 Domain 映射；05～08 维持现有功能，不提前彻底重构。
2. 十个排放源全部进入统一卡片体系：化石燃料燃烧 F01、原料煅烧 P01、焙烧/炭化 P02、石墨化 P03、烟气焚烧治理 P04A、烟气脱硫净化 P04B、购入电力 I01、购入热力/动力 I02、输出电力 I03、输出热力/动力 I04。
3. 建立可复用的 Presentation 卡片组件或统一配置机制，统一处理标题、状态、展开/折叠、摘要、错误/完成状态和启用/编辑/收起操作；不得复制十套互不相关的折叠逻辑。
4. “不涉及”默认折叠且不显示完整输入表单；用户应能在一屏或较短滚动范围内看到全部排放源状态。启用时映射为现有 `INVOLVED`、展开表单且不自动填造业务数据。
5. 已填写卡片收起后显示不含内部 ID、变量名和开发术语的简短业务摘要，例如“1 种燃料 · 填写中”“已完成”“2 条电力明细 · 已完成”。
6. 卡片展开→输入→折叠→再展开必须完整保留输入；继续保持 PR #2 的新建核算页面导航切换输入保留。不得引入草稿保存或跨启动恢复。
7. 原料煅烧、焙烧/炭化、石墨化等复杂卡片展开后按投入数据、产出数据、其他必要数据等业务含义分组，不得按 `gc / wfc / cc / ...` 内部变量顺序堆叠；所有标签继续来自 UIR01 `FieldSpec`。
8. 卡片化后继续使用 `field_specs.py` 和 `typed_inputs.py`；数量/百分比/整数输入限制、单位控制和百分比 0～100 到 Domain 0～1 转换不得退化。
9. I01 继续支持多条电力明细的新增、删除、独立取得方式、电力属性、证明/参数解析、独立快照和汇总；不得退化为单条输入。
10. “已完成”至少依据当前状态为 `INVOLVED`、阶段必要输入存在和 Presentation 基础校验无已知错误；启用但缺失必要输入可显示“填写中/需要处理”，不得复制或改写 Domain Validation。`UNCONFIRMED` 必须仍是待确认。
11. 不改变 `CarbonMaterialInput`、`CarbonMaterialCalculator`、Decimal 计算、GB/T 32151.34 公式、`ParameterResolver`、Canonical、参数/因子规则、records.sqlite、成功记录、ERROR 门禁、历史快照和删除审计。
12. 允许把 UIR03 的专业控件放入对应卡片内部以保持页面可用，但不得在本阶段改变物料/成分/归一化/固定碳/挥发分/证据/换算证明语义，不得实现专业详情隐藏或 UIR03 的简化规则。

#### OUT OF SCOPE

- 不实施 UIR03 的数据口径与换算简化、专业详情和参数高级信息隐藏；不实施 UIR04 的 05～08 彻底重构、最终错误跳转和 V1.1.0 收口。
- 不实施 Excel 导入、报告/导出、其他七项标准、企业档案、草稿保存、跨启动恢复、云服务、G09 或数据库迁移；应用版本不升级到 1.1.0。
- 不修改、删除或上传 `计算表/`，不打包标准全文 PDF/Word。

#### 测试与阶段门禁

- 必须新增 UIR02 专项测试，覆盖十卡片存在、默认不涉及折叠、启用映射与无自动数据、展开/折叠输入保持、导航输入保持、三类 Domain 状态映射、派生完成/需要处理状态、摘要无内部术语和多条电力不串位。
- 必须回归 `tests.test_uir01_field_semantics`、`tests.test_g06_page`、`tests.test_g06_carbon_material`、`tests.test_g07_records`、`tests.test_g08_delivery`，并验证 UIR01 数值键盘/粘贴限制和比例转换仍有效。
- 必须做 Domain parity：同一合法输入的 `CarbonMaterialInput` 关键值、`source_states`、`electricity_details`、计算结果和参数快照与卡片重构前等价，不得修改旧测试期望值掩盖行为变化。
- 完成前必须执行 UIR02 定向、相关回归、`unittest discover -s tests -t . -v`、compileall、pip check、Canonical 校验、git diff --check 和 `计算表/` 保护检查，并确认无测试数据库、临时文件、标准全文或敏感数据进入提交。
- UIR02 完成后更新 `TASK_STATE.md` 和 `IMPLEMENTATION_REPORT.md`，提交、推送独立分支、创建目标为 main 的 PR，等待最新 head 的 merge-ref full tests 与 exact PR-head standalone audit；停止等待 Sol 验收，不得启动 UIR03。

### 21.4 UIR03——交互辅助与错误呈现（未启动）

仅在 UIR02 经 Sol 验收通过后启动。不得在 UIR01 中改变 Domain 校验规则、记录生命周期或引入草稿/项目保存机制。

### 21.5 UIR04——可用性收口与回归（未启动）

仅在 UIR03 经 Sol 验收通过后启动。不得在 UIR01～UIR03 中把可用性收口扩展为 G09 或其他业务模块。
